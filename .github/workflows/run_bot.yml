name: 🧪 TEST SCHEDULE

on:
  # Test schedule: Run in 2 minutes from now
  schedule:
    - cron: '*/2 * * * *'  # Every 2 minutes
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: |
          echo "🧪 TESTING GITHUB SCHEDULES"
          echo "UTC: $(date -u)"
          echo "IST: $(TZ='Asia/Kolkata' date)"
          echo "If you see this, schedules work!"
